#include <stdio.h>

int main(void) {
    int number;  // 宣告變數

    printf("number\tsquare\tcube\n");  // 標題列

    number = 0;
    printf("%d\t%d\t%d\n", number, number * number, number * number * number);
    number = 1;
    printf("%d\t%d\t%d\n", number, number * number, number * number * number);
    number = 2;
    printf("%d\t%d\t%d\n", number, number * number, number * number * number);
    number = 3;
    printf("%d\t%d\t%d\n", number, number * number, number * number * number);
    number = 4;
    printf("%d\t%d\t%d\n", number, number * number, number * number * number);
    number = 5;
    printf("%d\t%d\t%d\n", number, number * number, number * number * number);
    number = 6;
    printf("%d\t%d\t%d\n", number, number * number, number * number * number);
    number = 7;
    printf("%d\t%d\t%d\n", number, number * number, number * number * number);
    number = 8;
    printf("%d\t%d\t%d\n", number, number * number, number * number * number);
    number = 9;
    printf("%d\t%d\t%d\n", number, number * number, number * number * number);
    number = 10;
    printf("%d\t%d\t%d\n", number, number * number, number * number * number);

    return 0;
}
